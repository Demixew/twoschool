# Телеграм-бот TwoSchool

## 🚀 Обзор

Телеграм-бот **TwoSchool** — это многофункциональный помощник для управления школьным расписанием. Он умеет хранить и показывать расписание, обрабатывать временные изменения (например, отмену уроков), отправлять ежедневные напоминания и анализировать учебную нагрузку. Бот использует нейросеть GigaChat для понимания запросов на естественном языке и предоставления "умных" ответов.

## ✨ Возможности

*   **🗓️ Управление расписанием**: Пользователи могут загружать свое расписание в форматах `.pdf`, `.xlsx` или `.ics`. Бот сохраняет его для дальнейшей работы.
*   **️ Умное распознавание текста (OCR)**: Бот умеет "читать" расписание с изображений (`.png`, `.jpg`). При обработке PDF-файлов он сначала пытается быстро извлечь текстовый слой, и только если его нет (например, в отсканированном документе), применяет технологию OCR для распознавания.
*   **⚡ Временные изменения**: Пользователи могут сообщать боту о разовых изменениях ("сегодня отменили литературу"), и он учтет их при ответах в течение дня.
*   **🔔 Ежедневные напоминания**: Автоматически отправляет сводку о расписании на следующий день, чтобы помочь пользователям быть в курсе.
*   **📲 Экспорт в .ICS**: Генерирует `.ics` файл с расписанием, который можно легко добавить в любой календарь (Google, Apple и др.).
*   **🐳 Развертывание в Docker**: Проект полностью готов к запуску в Docker-контейнере, что упрощает его развертывание и масштабирование.

## 🏗️ Архитектура

Бот создан на Python с использованием фреймворка **Aiogram 3** и состоит из следующих ключевых компонентов:

*   **`main.py`**: Точка входа приложения. Инициализирует бота, диспетчер, базу данных и планировщик задач (`apscheduler`).
*   **`bot/`**: Содержит всю логику, связанную с ботом:
    *   `handlers/`: Обработчики сообщений и команд от пользователя.
        *   `ask_schedule.py`: Обрабатывает вопросы о расписании и временные изменения.
        *   `schedule_upload.py`: Отвечает за загрузку и парсинг файлов с расписанием.
        *   `start.py`: Обрабатывает команду `/start` и выводит основную клавиатуру.
    *   `export.py`: Генерирует `.ics` файлы для экспорта.
*   **`services/`**: Вспомогательные сервисы, которые использует бот:
    *   `db.py`: Управляет асинхронным взаимодействием с базой данных `SQLite`.
    *   `gigachat.py`: Интеграция с API GigaChat для обработки NLP-запросов.
    *   `parser.py`: Содержит логику для разбора разных форматов файлов (`.pdf`, `.xlsx`, `.ics`), включая логику OCR с использованием `pytesseract` и `pdf2image`.
    *   `reminders.py`: Логика отправки ежедневных напоминаний.
    *   `cleanup.py`: Ежедневная очистка устаревших временных правок.
*   **`Dockerfile`**: Инструкция для сборки Docker-образа приложения.
*   **`docker-compose.yml`**: Файл для оркестрации и запуска сервиса с помощью Docker Compose.

## 🛠️ Установка и запуск

### 1. Предварительные требования

*   Python 3.10+
*   Tesseract OCR Engine (с поддержкой русского языка)
*   Poppler
*   Docker и Docker Compose

### 2. Клонирование репозитория

    ```sh
    git clone github.com/demixew/twoschool
    cd twoschool
    ```

### 3. Настройка переменных окружения

    Создайте файл `.env` в корне проекта и заполните его по примеру из `.env` (есть по умолчанию в корне проекта) или напрямую:

    ```env
    TELEGRAM_BOT_TOKEN="ВАШ_ТОКЕН_ТЕЛЕГРАМ_БОТА"
    GIGACHAT_AUTH="ВАШ_АВТОРИЗАЦИОННЫЙ_КЛЮЧ_GIGACHAT"
    ```

### 4. Установка зависимостей (локальный запуск)

    ```sh
    pip install -r requirements.txt
    ```

### 5. Запуск через Docker Compose (рекомендуемый способ)

    Этот способ автоматически соберет образ и запустит контейнер с ботом.

    ```sh
    docker-compose up --build -d
    ```

## 📖 Как пользоваться

1.  Отправьте боту команду `/start`.
2.  Загрузите свое расписание, отправив боту файл (`.pdf`, `.xlsx`, `.ics`) или просто **фотографию** расписания (`.png`, `.jpg`). Бот автоматически распознает текст и сохранит его.
4.  Сообщайте об изменениях: "Первый урок отменили", "Вместо истории будет обществознание".
5.  Используйте команду `/export`, чтобы получить файл для импорта в календарь.

## 📄 Лицензия

На данный момент у нас нет лицензии, но мы очень этого хотим.

## 🔮 Планы на будущее
*   **Умный парсинг распознанного текста**: Улучшить интеграцию с GigaChat для надежного извлечения структурированного расписания из "сырого" текста, полученного после OCR.
*   **Улучшение обработки естественного языка**: Добавить поддержку большего количества запросов и контекста.
*   **Интеграция с другими сервисами**: Подключить другие полезные сервисы для школьников, например, образовательные платформы или сервисы для подготовки к экзаменам.
*   **Персонализация напоминаний**: Дать возможность пользователям настраивать время и формат ежедневных напоминаний.
*   **Поддержка нескольких языков**: Добавить поддержку других языков, чтобы ботом могли пользоваться ученики из разных стран.
